now i have make titnic surival prediction model for classifcation example so in that i have many classification algorithms in which i have used the Logistic Regression,
DecisionTreeclassifer,RandomForestClawsdier,SVC,KneighboursClassfier,GuassianNB and among all these algorithm have difrent approaches to classify the predicted 
as LogisticRegression gives the probvalasity and on that basics it classfiy the predicted (Signmoid Fuction),
then DecisionTreeClassifier it ask question and then makes the decison and stores the predicted value in leaf node
and same RadnomForestC lassifer as ity makes manhy decision tree and then voting is performend in case of Classification.Then in case of SVC
It first finds the hyperplane (decision boundary to seperate the classes) and the  classify the points accoding to it and then 
at time of prediction it sees along with the class to which it belongs with thne in GuassianNB algorithm it predicts the class
on the basics of probability and first it assumes that all feature follows the guassian distribution and then at time of prediction it calutes the probability
and the highest probablity which is for that class it prediction is to be thereand for KNN in that it classify ther point randomly at time of  training and then at time of prediction it calcutaes the distance between
the old point and new point and then sorting it in ascending order and take top k values in case of classificatiuon it tkaes the majority for prediction of class of that data and for boosting alagorithm
it learns from the previous error and make the best prediction and i have used the xgb clasifier which learns from the previous model and combines all weak model and then it learns from the previous mistake



Rgression Problem
House price prediction
in this i have following algorithms LinearRegression(in which y =mx+b) follows it this rule and predicts the house price
and and then Ridge Regression(l2 regularization) whihc miminzes the coeficcient weight by doing sum of squarred of error and then lasso Regression which removes the featuire by doing the exact zero value and then decision tree Regressor it ask question and then makes the decison and stores the predicted value in leaf node
and same RadnomForestClassifer as ity makes manhy decision tree and then voting is performend in case of Classification.
and then in case of svr hen in case of SVC
It first finds the hyperplane (decision boundary to seperate the classes) and the  classify the points accoding to it
and the KneighboursRegressor in whcih in that it classify ther point randomly at time of  training and then at time of prediction it calcutaes the distance between
the old point and new point and then sorting it in ascending order and take top k values in case of classificatiuon it tkaes the majority for prediction of class of that data.

